import { LocationNameFilterPipe } from './location-name-filter.pipe';

describe('LocationNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
